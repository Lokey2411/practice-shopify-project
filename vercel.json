{
	"version": 2,
	"builds": [
		{
			"src": "src/index.ts",
			"use": "@vercel/node",
			"config": {
				"buildCommand": "npm run build",
				"outputDirectory": "dist",
				"includeFiles": ["tsconfig.json"]
			}
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "dist/index.js"
		}
	]
}
